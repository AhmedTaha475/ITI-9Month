#include <stdio.h>
#include<conio.h>
void main()
{
    printf("Please enter a character\n");
    char x=getchar(); //takes the char from the user and waits for enter to take action
    //char x = getch(); //takes the char from the user and goes to the next action without pressing enter
    //char x =getche(); //takes the char from the user and display it without using enter
     printf("\n%d",x);

}
